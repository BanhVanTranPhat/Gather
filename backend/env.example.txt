# Copy this file to .env and fill in your values.
# Required for minimal run: PORT, MONGODB_URI, JWT_SECRET, CLIENT_URL, EMAIL_* (for OTP).

# --- Server ---
PORT=5001
NODE_ENV=development
# LOG_LEVEL=debug
# DEFAULT_ROOM_CAPACITY=20

# --- Database ---
MONGODB_URI=mongodb://localhost:27017/gather-town

# --- Auth ---
# CHANGE THIS IN PRODUCTION!
JWT_SECRET=your-very-secret-key-change-this-in-production

# --- CORS / Frontend ---
CLIENT_URL=http://localhost:5173
# Extra origins (comma-separated)
CLIENT_URLS=
# Netlify: site name only (e.g. gather-demo). Previews: deploy-preview-123--<name>.netlify.app
NETLIFY_SITE_NAME=
ALLOW_NETLIFY_PREVIEWS=false

# --- Email (OTP) – Gmail: use App Password
# https://support.google.com/accounts/answer/185833
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# --- Google OAuth ---
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- Google reCAPTCHA (optional) ---
GOOGLE_RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key

# --- WebRTC / Mediasoup (optional, for voice/video) ---
# WEBRTC_LISTEN_IP=0.0.0.0
# WEBRTC_ANNOUNCED_IP=
# WEBRTC_MIN_PORT=40000
# WEBRTC_MAX_PORT=49999
# WEBRTC_INITIAL_BITRATE=800000
# MEDIASOUP_LOG_LEVEL=warn
# MEDIASOUP_LOG_TAGS=